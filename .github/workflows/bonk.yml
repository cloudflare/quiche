name: Bonk

on:
  pull_request:
    types: [opened]
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.event.issue.number || github.ref }}
  cancel-in-progress: false

jobs:
  bonk:
    # Runs in two situations:
    #  - automatically when a PR is opened from a branch in this repository
    #    (fork-triggered runs don't receive the CF_AI_GATEWAY_* secrets), or
    #  - on demand when an org member (OWNER/MEMBER) comments /bonk or @ask-bonk.
    if: >-
      github.event.sender.type != 'Bot' && (
        (github.event_name == 'pull_request' &&
         github.event.pull_request.head.repo.full_name == github.repository) ||
        (github.event_name != 'pull_request' &&
         contains(fromJson('["OWNER","MEMBER"]'), github.event.comment.author_association) &&
         (contains(github.event.comment.body, '/bonk') || contains(github.event.comment.body, '@ask-bonk')))
      )
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      id-token: write
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 1
          persist-credentials: false

      - name: Run Bonk
        uses: ask-bonk/ask-bonk/github@main
        env:
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CF_AI_GATEWAY_ACCOUNT_ID }}
          CLOUDFLARE_GATEWAY_ID: ${{ secrets.CF_AI_GATEWAY_NAME }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CF_AI_GATEWAY_TOKEN }}
        with:
          model: "cloudflare-ai-gateway/openai/gpt-5.6-sol"
          variant: "xhigh"
          mentions: "/bonk,@ask-bonk"
          # Only users with write access may invoke Bonk via comment.
          permissions: write
          # Review-only: Bonk leaves comments/suggestions but never pushes commits.
          token_permissions: NO_PUSH
          opencode_version: "latest"
          prompt: |
            Review this pull request. Summarize what it changes and flag any
            correctness, security, or style issues as inline review comments.
            Do not push commits.
